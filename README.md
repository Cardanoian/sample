# VIBE//REBOOT

바이브 코딩 연수생을 위한 사이버펑크 포스트 아포칼립스 웰컴 페이지입니다.

## 로컬 실행

```bash
python3 -m http.server 4173
```

브라우저에서 `http://localhost:4173`을 엽니다.

## 배포

`main` 또는 `work` 브랜치에 push하면 GitHub Actions가 정적 파일을 GitHub Pages에 배포합니다. 저장소의 **Settings → Pages → Source**가 **GitHub Actions**로 설정되어 있어야 합니다.
